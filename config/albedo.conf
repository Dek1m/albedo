# Конфигурация albedo
# Формат: KEY=VALUE
# Комментарии начинаются с #
# Файл монтируется в контейнер: /etc/albedo/albedo.conf
# Приоритет: environment / .env  >  этот файл  >  дефолты entrypoint

ALBEDO_LISTEN_HOST=0.0.0.0
ALBEDO_LISTEN_PORT=8080
ALBEDO_API_URL=http://belle:8080
